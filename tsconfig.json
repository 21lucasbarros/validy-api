{
  "compilerOptions": {
    /* ====== Básico ====== */
    "target": "ES2021" /* Define a versão de JS gerada */,
    "module": "ESNext" /* Módulos compatíveis com Node moderno */,
    "moduleResolution": "node" /* Resolve imports no estilo Node */,
    "rootDir": "src" /* Código-fonte fica em src/ */,
    "outDir": "dist" /* Código compilado vai pra dist/ */,
    "types": ["node"] /* Usa tipos do Node, não do Bun */,

    /* ====== Compatibilidade ====== */
    "esModuleInterop": true /* Suporte a import/export modernos */,
    "forceConsistentCasingInFileNames": true /* Garante consistência nos imports */,
    "skipLibCheck": true /* Ignora checagem de tipos em libs */,
    "strict": true /* Ativa verificações rigorosas */,

    /* ====== JSX (se quiser usar depois) ====== */
    "jsx": "react" /* Mantido igual ao teu original */
  },
  "include": ["src"] /* Inclui a pasta src */,
  "exclude": [
    "node_modules",
    "dist"
  ] /* Evita recompilar pastas desnecessárias */
}
